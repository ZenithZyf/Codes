# queue: first in first out
# double－ended queue is a built-in data structure in python

# deque
a = deque()
# join in append: join in from the right
a.append()
# join in left: appendleft
a.appendleft()
# removes element from right
a.pop()
# removes element from left
a.popleft()
# add list
a.extend()
a.extendleft()

# linked list
# they are linked from adjacent elements
# value and (next element's) address pair
class node:
	self.value = 1
	self.next = ...
	

